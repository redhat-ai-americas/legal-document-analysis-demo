apiVersion: v1
kind: Service
metadata:
  name: legal-doc-ui
  labels:
    app: legal-doc-analysis
    component: ui
spec:
  type: ClusterIP
  ports:
  - port: 8501
    targetPort: 8501
    protocol: TCP
    name: http
  selector:
    app: legal-doc-analysis
    component: ui